name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - dev

jobs:
  # ─────────────────────────────────────────────
  # Job 1: Static quality gates (lint, type, build)
  # ─────────────────────────────────────────────
  quality:
    name: Lint · Typecheck · Unit Tests · Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit & Integration Tests
        run: npx vitest run
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/shackles_test
          SESSION_SECRET: ci-test-session-secret-32-characters-x
          ACTIVE_YEAR: "2026"
          ACTIVE_THEME_KEY: default
          ACTIVE_PUBLIC_DOMAIN: localhost
          STORAGE_PROVIDER: local

      - name: Build
        run: npm run build
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/shackles_test
          SESSION_SECRET: ci-build-session-secret-32-characters
          ACTIVE_YEAR: "2026"
          ACTIVE_THEME_KEY: default
          ACTIVE_PUBLIC_DOMAIN: localhost
          STORAGE_PROVIDER: local

      - name: Ensure Prisma migrations folder exists
        run: |
          if [ ! -d prisma/migrations ]; then
            echo "Missing prisma/migrations. Run 'npx prisma migrate dev --name baseline' and commit."
            exit 1
          fi

      - name: Migration drift check
        run: npm run db:drift:check

      - name: Phase 6 governance artifact check
        run: npm run phase6:governance:check

      - name: Phase 6 freeze policy check
        env:
          RELEASE_FREEZE_ACTIVE: ${{ vars.RELEASE_FREEZE_ACTIVE }}
          PHASE6_FREEZE_EXCEPTION: ${{ vars.PHASE6_FREEZE_EXCEPTION }}
        run: npm run phase6:freeze:check

  # ─────────────────────────────────────────────
  # Job 2: Playwright E2E (runs on PRs + main push)
  # Skipped on dev pushes to keep feedback fast.
  # ─────────────────────────────────────────────
  e2e:
    name: Playwright E2E
    runs-on: ubuntu-latest
    needs: quality
    if: github.ref == 'refs/heads/main' || github.event_name == 'pull_request'

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: shackles_e2e
        ports:
          - 5432:5432
        options: >
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Apply database migrations
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/shackles_e2e

      - name: Run E2E tests
        run: npx playwright test
        env:
          CI: "true"
          PLAYWRIGHT_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/shackles_e2e
          PLAYWRIGHT_SESSION_SECRET: ci-e2e-session-secret-32-characters-xx
          ACTIVE_YEAR: "2026"
          ACTIVE_THEME_KEY: default
          ACTIVE_PUBLIC_DOMAIN: localhost
          STORAGE_PROVIDER: local
          NEXT_PUBLIC_APP_URL: http://127.0.0.1:3100

      - name: Upload Playwright report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: test-results/
          retention-days: 7
